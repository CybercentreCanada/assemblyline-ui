core:
  alerter:
    delay: 0
  metrics:
    elasticsearch:
      hosts: [localhost]
    redis:
      host: localhost
  redis:
    nonpersistent:
      host: localhost
    persistent:
      host: localhost
      port: 6379

datastore:
  elasticsearch:
    nodes: [localhost]
  hosts: [localhost]

filestore:
  cache: ["file:///tmp/cache/"]
  storage: ["file:///tmp/storage/"]

logging:
  log_to_console: false
  log_to_file: false
  log_to_syslog: false